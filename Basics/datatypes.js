"use strict";  // treat all JS code as newer version

// alert(3 + 3); // we are using nodejs, not browser, so alert won't work

console.log(3 + 3);

let name = "Achyut";
let age = 25;
let isloggedIn = true;
let state;



// number => 2 to power 53 - 1
// bigint => 2n to power 53 - 1
// string => "hello"
// boolean => true/false
// null => standalone value, null 
// undefined => standalone value, (value not assigned)
// symbol => unique


// object => {key: value}

console.log(typeof name);
console.log(typeof age);
console.log(typeof null); //object
console.log(typeof undefined); //undefined